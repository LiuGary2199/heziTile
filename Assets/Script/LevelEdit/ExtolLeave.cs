using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ExtolLeave : MonoBehaviour
{
[UnityEngine.Serialization.FormerlySerializedAs("empty_show")]    [UnityEngine.Serialization.FormerlySerializedAs("empty_show")]public bool Dimly_Barn= false;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
